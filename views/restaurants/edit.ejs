<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Restaurant</title>
</head>

<body>
    <h1>Edit Restaurant</h1>

    <!-- _method=PUT tells Express (via method override) to treat this as a  -->
    <!-- PUT request, instead of a POST request.                             -->
    <form action="/restaurants/<%= restaurant._id %>?_method=PUT" method="POST">
        <div>
            <label for="title">Title</label>
            <input type="text" id="title" name="restaurant[title]" value="<%= restaurant.title %>">
        </div>

        <div>
            <label for="location">Location</label>
            <input type="text" id="location" name="restaurant[location]" value="<%= restaurant.location %>">
        </div>

        <button>Update Restaurant</button>
    </form>

    <a href="/restaurants/<%= restaurant._id %>">Back to Restaurant</a>
</body>

</html>
