<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Show</title>
</head>

<body>
    <h1><%= restaurant.title %></h1>
    <h2><%= restaurant.location %></h2>

    <footer>
        <div>
            <a href="/restaurants/<%= restaurant._id %>/edit">Edit</a>
        </div>

        <!-- _method=DELETE tells Express (via method override) to treat     -->
        <!-- this as a DELETE request, instead of a POST request.            -->
        <div>
            <form action="/restaurants/<%= restaurant._id %>?_method=DELETE" method="POST">
                <button>Delete</button>
            </form>
        </div>

        <div>
            <a href="/restaurants">All Restaurants</a>
        </div>
    </footer>
</body>

</html>
